#####################################################################
#====================================================================
# TABELAS
#====================================================================
frequencia_biblioteca <- table(df[1])
metodo_de_consulta <- table(df[2])
rapidez_sis_manual <- table(df[3])
achar_inconsistencias <- table(df[4])
beneficios_sis_automatico <- table(df[5])
pref_consulta <- table(df[6])
medida_melhora <- table(df[7])
#====================================================================
#####################################################################
#====================================================================